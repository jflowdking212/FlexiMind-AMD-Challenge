# Challenge Source

Challenge-specific implementation will be added here.

Suggested structure after the AMD environment is verified:

```text
src/
├── amd/
│   ├── adapter.ts
│   ├── types.ts
│   └── health.ts
├── evaluation/
│   ├── verifier.ts
│   └── cases.ts
└── examples/
    └── run-demo.ts
```

The exact implementation should follow the real AMD environment rather than being prematurely locked to an unverified API or runtime.
